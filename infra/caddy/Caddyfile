# Tunnel mode: TLS terminates at Cloudflare's edge; cloudflared delivers
# requests to Caddy over the compose network on plain :80. Caddy reverse-proxies
# the API only — the SPA is served separately by Cloudflare Pages (see
# DEPLOYMENT.md), so Caddy no longer serves static files. No ACME, no open ports.
{
  auto_https off
}

http://{$API_HOST:eventform-api.murugappan.dev} {
  reverse_proxy api:3001
}
